const navigateToPage = require('./navigateToPage');
const fillMultiSelect = require('./fillMultiSelect');
const selectDropdown = require('./selectDropdown');
const selectDate = require('./selectDate');

module.exports = {
  navigateToPage,
  fillMultiSelect,
  selectDropdown,
  selectDate
};
